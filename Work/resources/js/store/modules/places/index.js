import getters from './getters'
import mutations from './mutations'
import actions from './actions'

const state = {
  places_full: null,
  places_combobox: null,
}

export default {
  state,
  getters,
  mutations,
  actions
}